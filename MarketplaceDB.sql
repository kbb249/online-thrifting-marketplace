CREATE DATABASE marketplace_db;
USE marketplace_db;

CREATE TABLE `marketplace_db`.`user_account` (
  `Email` VARCHAR(50) NOT NULL,
  `Password` VARCHAR(45) NOT NULL,
  PRIMARY KEY (`Email`));
  show schemas;
  show tables;